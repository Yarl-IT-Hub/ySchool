<project name="yschool (Hibernate Pojo Generator)" default="compile" basedir=".">
<!-- Build the CLASSPATH -->
	<path id="classpath">
        <fileset dir="../libs" includes="**/*.jar" />
    </path>
	<path id="classpath.test">
        <path refid="classpath" />
        <pathelement location="bin" />
    </path>
	<target name="clean">
		<delete dir="../bin"/>
		<delete dir="../dist"/>
		<delete file="./yschool.zip"/>
	</target>
    <target name="compile" description="Compiles the source code generated by the hibernate pojo generator" depends="clean">
    	<mkdir dir="../bin"/>
    	<javac srcdir=".." fork="yes" destdir="../bin" classpathref="classpath.test" debug="on" />
        <copy file="applicationContext.xml" todir="../bin" />
    </target>
	<target name="jar" description="JARs the compiled sources" depends="compile">
		<mkdir dir="../dist"/>
		<jar basedir="../bin" destfile="../dist/yschool.jar"/>
	</target>
	<target name="javadoc" description="Builds the javadoc of the generated sources" depends="compile">
		<mkdir dir="../dist/docs"/>
		<javadoc classpathref="classpath" sourcepath="src" destdir="../dist/docs"/>
		<zip basedir="../dist/docs" destfile="../dist/javadocs.zip"/>
		<delete dir="../dist/docs"/>
	</target>
</project>